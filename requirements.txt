# SignalKit - all dependencies in one venv
# Install: pip install setuptools<81 && pip install --no-build-isolation -r requirements.txt

# Qt/QML display
PySide6>=6.10

# OBD2 communication
obd>=0.7
pyserial>=3.5

# Web API server
Flask>=3.1

# ELM327 emulator (dev/preview)
setuptools<81            # ELM327-emulator needs pkg_resources at runtime
ELM327-emulator>=3.0
